# Copy this file to .env and fill in real values. Never commit .env itself.

# Gemini API key (Google AI Studio, free tier, no credit card)
GEMINI_API_KEY=your_key_here

# Postgres connection string (local Docker instance is fine)
DATABASE_URL=postgresql://user:password@localhost:5432/imagerelevance

# App settings
ENVIRONMENT=development
